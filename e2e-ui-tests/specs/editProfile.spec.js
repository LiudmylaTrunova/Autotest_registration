/*const { test, expect, request } = require('@playwright/test');
//const { LoginPage } = require('../pages/loginPage');
const { RegistrationPage } = require('../pages/registrationPage');
const { EditProfilePage } = require('../pages/editProfilePage');
const { users } = require('../../test-data/users');
const { userData } = require('../../test-data/data');
const config = require("../../playwright.config");*/

/*let EditProfilePage;
const env = config.default.use.env;


test.describe('EditProfile tests', () => {

    test.beforeEach(async ({ page }) => {
        loginPage = new LoginPage(page);
        await loginPage.open();
    });

    test('EditProfile with valid data', async ({ page }) => {
        await EditProfilePage.EditProfile(users[env].user.email, users[env].user.password);
        await expect(page.locator("[class='logo']")).toBeVisible;
    });

});*/
